{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "ArgsDebug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/C04/ex04/a.out", // Path to your compiled executable
            "args": ["13", "01"], 
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb" // Ensure this path matches your GDB installation
        }
    ]
}